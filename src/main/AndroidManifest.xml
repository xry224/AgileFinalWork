<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.example.agile">

    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:icon="@mipmap/ic_launcher"
                 android:roundIcon="@mipmap/ic_launcher_round"
                 android:supportsRtl="true"
                 android:theme="@style/Theme.AppCompat.Light.NoActionBar"
                 tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="Activity.findNearby">
        </activity>
        <activity android:name="Activity.historyList">
        </activity>
        <activity android:name="Activity.myAccount">
        </activity>
        <activity
                android:name="Activity.MainActivity"
                android:label="出游" >
         </activity>
        <activity android:name="Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Activity.EventDetail"/>
        <activity android:name="Activity.newEventActivity"/>
        <activity android:name="Activity.Register"/>
        <activity android:name="Activity.ForgetPW"/>
        <activity android:name="Activity.MerchantDetail"/>
        <activity android:name="Activity.MessageActivity"/>
    </application>
</manifest>
