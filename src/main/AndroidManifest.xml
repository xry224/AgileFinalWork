<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.example.agile">

    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:icon="@mipmap/ic_launcher"
                 android:roundIcon="@mipmap/ic_launcher_round"
                 android:supportsRtl="true"
                 android:theme="@style/Theme.AppCompat.Light.NoActionBar" tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="View.findNearby">
        </activity>
        <activity android:name="View.historyList">
        </activity>
        <activity android:name="View.myAccount">
        </activity>
        <activity
                android:name="View.MainActivity"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar"
                android:label="出游" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
         </activity>
    </application>
</manifest>
